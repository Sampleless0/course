# variables naming hehe

name = True # allowed

1name = False # NOT allowed as numbers cannot be at the front
name1 = True # allowed

_name = True # allowed
name_ = True # allowed

%name = False # NOT allowed as only _ can be used
name% = False # NOT allowed

def = True # NOT allowed as def is a reserved keyword
# some reserved keywords that u should know about (u cannot name ur variables as these)
False      await      else       import     pass
None       break      except     in         raise
True       class      finally    is         return
and        continue   for        lambda     try
as         def        from       nonlocal   while
assert     del        global     not        with
async      elif       if         or         yield
